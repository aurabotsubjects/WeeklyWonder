// Weekly Wonders configuration
//
// If you host the GIFs somewhere other than this repo's own
// assets/gifs folder (e.g. a Cloudflare R2 bucket), set ASSET_BASE_URL
// to that bucket's public base URL (no trailing slash). Leave it as an
// empty string to load GIFs from the local assets/gifs folder that
// ships with this repo.
//
// The bucket must contain the files at the same relative path they
// have here, i.e. "assets/gifs/w1_f1.gif", "assets/gifs/w2_f1.gif", etc.
// so this base URL just gets prepended to that path.
//
// Example:
// window.ASSET_BASE_URL = "https://pub-1a2b3c4d5e6f7g8h.r2.dev";
window.ASSET_BASE_URL = "";
